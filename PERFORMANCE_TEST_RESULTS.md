# 성능·부하 테스트 기록

이 문서는 6단계 기준값과 향후 8단계 개선 결과를 같은 조건으로 비교하기 위한 기록입니다.

## 기준선 A. 동일 이미지 중복 제출

- 목적: 동일 이미지가 하나의 Receipt와 ExtractionJob으로 수렴하는지, Redis 동일 키 락에서 어느 정도까지 처리되는지 확인
- 데이터: 65KB 비민감 합성 JPEG 한 장
- 결과: 요청 손실 없이 Receipt 1건, ExtractionJob 1건, Job COMPLETED

| 프로필 | 요청 수 | 처리량 | P95 | P99 | 실패율 |
|---|---:|---:|---:|---:|---:|
| Smoke, 5 VU × 10초 | 7,179 | 716.04 req/s | 20.31ms | 31.88ms | 0% |
| Normal, 20 VU × 30초 | 24,682 | 822.10 req/s | 92.08ms | 162.45ms | 0% |
| Heavy, 50 VU × 60초 | 48,103 | 800.86 req/s | 271.64ms | 451.76ms | 0% |
| Stress, 100 VU × 60초 | 34,173 | 568.18 req/s | 398.08ms | 578.45ms | 0% |
| Spike, 200 VU × 30초 | 17,368 | 573.60 req/s | 824.38ms | 1.21s | 0% |

100 VU에서 200 VU로 증가해도 처리량은 약 570 req/s로 정체되고 P95는 약 2.1배 증가했습니다. CPU·DB 풀·Job 적체에는 여유가 있어 동일 이미지 Redis 락 직렬화를 우선 병목 후보로 기록합니다.

## 기준선 B. 200명·고유 이미지 200건

- 목적: 200명이 서로 다른 영수증을 동시에 제출할 때 접수 유실, API 지연, Job 적체와 최종 수렴을 확인
- 데이터: 하나의 비민감 합성 JPEG 뒤에 VU 식별 바이트를 추가해 이미지 디코딩은 유지하고 SHA-256만 200개로 분리
- 실행: `docker compose run --rm -e USERS=200 k6 run k6-unique-upload.js`

| 항목 | 결과 |
|---|---:|
| 동시 사용자 | 200 VU |
| 고유 이미지·Receipt·Job | 각각 200건 |
| 접수 완료 시간 | 약 0.5초 |
| 접수 처리량 | 419.16 req/s |
| 평균 응답시간 | 289.55ms |
| P95 / P99 | 409.82ms / 447.19ms |
| 접수 실패율 | 0% |
| 최대 미완료 Job | 188건 |
| 전체 Job 완료 시간 | 51.237초 |
| 최종 상태 | COMPLETED 200건, AUTO_APPROVED 200건 |
| 실패·유실·중복 | 각각 0건 |
| 감사 이벤트 | 800건 |

접수 API는 200건을 빠르게 MySQL 작업 큐에 저장했지만, Fake 추출기가 즉시 응답하는 환경에서도 Worker가 한 번에 4건을 선점하고 다음 1초 polling까지 기다려 전체 완료에 약 51초가 걸렸습니다. 현재 설정에서 작업 소비 속도가 약 4건/초로 제한되는 기준선입니다.

## 8단계 개선 후 비교

측정 환경과 k6 조건은 기준선과 동일하며, 실제 영수증이나 OpenAI 키 없이 Fake 추출기와 비민감 합성 JPEG를 사용했습니다.

### A. 동일 이미지 200 VU × 30초

| 항목 | 개선 전 | 개선 후 | 변화 |
|---|---:|---:|---:|
| 완료 요청 | 17,368건 | 55,966건 | 3.22배 |
| 처리량 | 573.60 req/s | 1,863.67 req/s | 3.25배 |
| 평균 응답시간 | 346.45ms | 106.84ms | 69.2% 감소 |
| P95 | 824.38ms | 242.90ms | 70.5% 감소 |
| P99 | 1.21초 | 350.52ms | 약 71.0% 감소 |
| 실패율 | 0% | 0% | 동일 |

최초 중복 감지와 감사 기록이 끝난 요청은 `duplicateDetected`를 확인해 Redis 락을 다시 기다리지 않고 기존 Receipt와 Job을 반환합니다. 최초 동시 진입에서 Redis 락 대기 시간이 초과돼도 MySQL에 중복 처리가 끝난 행이 확인되면 기존 결과로 수렴시킵니다.

DB 검증 결과 55,966건은 Receipt 1건, ExtractionJob 1건, `DUPLICATE_DETECTED` 감사 이벤트 1건으로 수렴했습니다. Redis 락과 MySQL Unique Constraint는 새 이미지 생성 경로의 1차·최종 방어선으로 유지했습니다.

### B. 200명·고유 이미지 200건

| 항목 | 개선 전 | 개선 후 | 변화 |
|---|---:|---:|---:|
| 접수 처리량 | 419.16 req/s | 563.42 req/s | 34.4% 증가 |
| P95 | 409.82ms | 271.09ms | 33.9% 감소 |
| P99 | 447.19ms | 292.65ms | 34.6% 감소 |
| 전체 Job 완료 시간 | 51.237초 | 1.5757초 | 96.9% 감소, 32.5배 단축 |
| 실패율 | 0% | 0% | 동일 |

Worker는 정기 polling에서 한 번 선점한 뒤 각 작업이 끝나 빈 슬롯이 생길 때마다 다음 Job 한 건을 즉시 보충합니다. Semaphore는 실행 중과 짧은 인계 대기 중인 작업의 합계를 `concurrency` 이하로 유지합니다.

최종 DB 결과는 고유 Receipt·Job 각각 200건, `COMPLETED`·`AUTO_APPROVED` 각각 200건, 감사 이벤트 800건입니다. 전체 처리가 Prometheus의 5초 scrape 간격보다 빠르게 끝나 일시적 최대 적체는 시계열에 포착되지 않았고, 완료 시간은 MySQL의 최초 `created_at`과 최종 `completed_at` 차이로 계산했습니다.

Grafana 대시보드에는 위 고정 기준값의 처리량, P95와 200 Job 완료 시간을 전후 비교하는 패널을 추가했습니다. 상세 수치는 이 문서를 기준 기록으로 사용합니다.
